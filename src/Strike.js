/**
 * Renders a strike element.
 * @param {Object} props - The component props.
 * @param {string} props.className - The CSS class name for the strike element.
 * @returns {JSX.Element} The strike element.
 */
export function Strike({ className }) {
  return <div className={className}></div>;
}
